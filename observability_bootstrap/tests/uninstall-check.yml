---
- hosts: localhost
  become: true
  vars:
    opentelemetry_state: absent
    tempo_state: absent
    promtail_state: absent
    loki_state: absent
    grafana_state: absent
    prometheus_state: absent
    nginx_state: absent
  roles:
  - ../roles/nginx
  - ../roles/prometheus
  - ../roles/grafana
  - ../roles/loki
  - ../roles/promtail
  - ../roles/tempo
  - ../roles/opentelemetry
